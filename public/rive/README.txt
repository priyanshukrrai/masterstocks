Drop a compiled Rive file named `button.riv` in this folder to enable the
fully interactive Rive button (state machine name: "State Machine 1").

Without this file the app automatically falls back to a Framer Motion animated
button, so the project runs fine either way.

Make your own at https://rive.app and export a .riv with a state machine.
